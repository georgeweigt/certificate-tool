Functions for analyzing certificate files.

The main function is a demo.

To build and run:

	cd src
	make
	./ctool

To check a self-signed certificate's signature:

	./ctool check cert.pem

To print a private key:

	./ctool key key.pem

There are scripts in the tools directory for creating certificates.

Supported public key algorithms:

	RSA
	prime256v1 (NIST P-256)
	secp384r1 (NIST P-384)

Supported hash algorithms:

	MD5
	SHA1
	SHA224
	SHA256
	SHA384
	SHA512
