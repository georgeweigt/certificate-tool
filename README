C code for processing certificates.

The main function is a demo.

To build and run:

	cd src
	make
	./ctool

Check that certificate A is signed by B:

	./ctool check A.pem B.pem

Print keys:

	./ctool key key.pem

Sign certificate A with B and save as C:

	./ctool sign A.pem B.pem key.pem | tee C.pem

There are scripts in the tools directory for creating certificate files.

Example:

	cd tools
	./m2
	../src/ctool sign a.pem b.pem key.pem | tee c.pem
	../src/ctool check c.pem b.pem

Supported public key algorithms:

	RSA
	prime256v1 (NIST P-256)
	secp384r1 (NIST P-384)

Supported hash algorithms:

	MD5
	SHA1
	SHA224
	SHA256
	SHA384
	SHA512

Lines per C file:

     129 base64.c
      87 check_signature.c
    2455 ec.c
     246 main.c
     231 md5.c
     773 parse_certificate.c
     275 parse_key_data.c
     221 pkcs.c
      85 read_certificate.c
     196 read_key_file.c
     539 rsa.c
     236 sha1.c
     230 sha224.c
     230 sha256.c
     250 sha384.c
     250 sha512.c
     509 sign_certificate.c
